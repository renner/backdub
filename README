Backdub is a generic backup solution that can be used for archiving any kind of data.

Getting Started:

- Create a configuration file (see 'config.template')
- Compile a targetfile containing filesystem paths (see 'target.example')
- Perform a backup of those files using './backup.py <targetfile>'
